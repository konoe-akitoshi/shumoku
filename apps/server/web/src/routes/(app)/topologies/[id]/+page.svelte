<script lang="ts">
  /**
   * The Diagram index route. The canvas itself is rendered persistently by
   * the shell layout (`TopologyCanvas`, kept mounted behind the Composition
   * drawer), so this route is intentionally empty — being here just means
   * "drawer closed, looking at the diagram." See
   * `apps/server/docs/design/topology-ui-ia.md`.
   */
</script>
