# Legacy documentation sources

Original website MDX is retained here during the SvelteKit replacement. This is
an archive, not the source for the current Docs site. Do not delete pending or
partial entries from `../migration.routes.json` just to pass the migration check.
Ready routes redirect to Docs; other known routes link to these archived sources.
The generated server/editor snapshots are collected only for inventory checks.
