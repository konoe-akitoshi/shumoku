---
---

huawei-nce-campus: migrate to the shared plugin-sdk httpClient and add a per-datasource "Skip TLS verification" option (on-prem NCE controllers serve Huawei's self-signed platform cert). Private workspace package — no npm release.
